<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunyard.sunafm.mapper.AfmImageDupAssocMapper">

    <insert id="insertBatch" parameterType="java.util.List">
        insert into afm_image_dup_assoc(id, dup_note_id,
        assoc_exif_id,similarity,create_time)
        values
        <foreach collection="list" item="it" separator=",">
            (#{it.id}, #{it.dupNoteId}, #{it.assocExifId},#{it.similarity}, #{it.createTime})
        </foreach>
    </insert>


</mapper>
